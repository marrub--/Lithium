// Score values
const int Score_Clip      = 1000;
const int Score_ClipBox   = 4000;
const int Score_Shell     = 2000;
const int Score_ShellBox  = 5000;
const int Score_Rocket    = 3000;
const int Score_RocketBox = 6000;
const int Score_Cell      = 4000;
const int Score_CellPack  = 7000;
const int Score_Backpack  = (Score_Clip + Score_ShellBox + Score_Rocket + Score_Cell) * 5;

// Tier 1
const int Score_ZombieMan     = 1000; // Bullets
const int Score_ShotgunGuy    = 2000; // Bullets
const int Score_Imp           = 2000; // Fire
const int Score_ChaingunGuy   = 2000; // Bullets

// Tier 2
const int Score_Demon         = 5000; // Melee
const int Score_LostSoul      = 500;  // Melee
const int Score_Nitrogolem    = 4000; // Melee, FireMagic

// Tier 3
const int Score_HellKnight    = 7000; // Melee, Magic
const int Score_Revenant      = 7000; // Melee, Shrapnel
const int Score_Cacodemon     = 7000; // Melee, Energy
const int Score_Arachnotron   = 7000; // Energy
const int Score_Mancubus      = 7000; // Fire
const int Score_BaronOfHell   = 8000; // Melee, Magic

// Tier 4
const int Score_PainElemental = 20000; // None
const int Score_Archvile      = 40000; // FireMagic

// Tier 5
const int Score_SpiderDemon   = 700000;  // Bullets
const int Score_CyberDemon    = 1500000; // Shrapnel

// Tier 6
const int Score_DSparil       = 10000000; // Energy

// EOF

